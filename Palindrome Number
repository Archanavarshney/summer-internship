class Solution {
public:
    bool isPalindrome(int x) {
        int orginal = x; if(x<0)
 {
 return (false);
 }
 int res =0;
 int rem;
 while(x!=0)
 {
 rem = x%10;
 res = res*10 + rem ;
 x = x/10;
 
 }
 if(res==orginal)
 {
 return(true);
 }
 else
 {
 return(false);
 }
 
 }
};
